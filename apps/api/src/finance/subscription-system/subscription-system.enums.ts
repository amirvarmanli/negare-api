export enum SubscriptionStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  CANCELED = 'CANCELED',
}

export enum SubscriptionDownloadType {
  FREE = 'FREE',
  SUBSCRIPTION = 'SUBSCRIPTION',
  PURCHASED = 'PURCHASED',
}
